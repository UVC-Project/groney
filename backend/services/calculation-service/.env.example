# Calculation Service Configuration
PORT=3006
NODE_ENV=development

# Database
DATABASE_URL=postgresql://groney_user:groney_password@postgres:5432/groney

# CO2 Calculation Factors (kg CO2 per unit), placeholder values for now
CO2_FACTOR_WATERING=0.5
CO2_FACTOR_WEEDING=0.3
CO2_FACTOR_PLANTING=1.0
CO2_FACTOR_CLEANING=0.2
CO2_FACTOR_FEEDING=0.1
